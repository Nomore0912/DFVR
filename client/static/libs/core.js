/**
 * init pdb
 */
var df;
df = {

    // all pdb info group, 这里包含 pdb 用于展示的全部属性
    GROUP: {},
    GeoCenterOffset: "",

    // residue
    residue: '',

    exportPDB: false,
    // 0: 3D mode, 1: vr mode,
    mode: 1,
    //Mode
    MODE_THREE: 0,
    MODE_VR: 1,
}
